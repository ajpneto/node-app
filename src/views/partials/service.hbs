        <!-- Service Start -->
        <div class="container-xxl py-0">
            <div class="container">
                <div class="row g-4">{{#each this}}{{#each items}}
                    <div class="border-0 col-lg-3 col-sm-6 wow slideInLeft" data-wow-duration="2s" data-wow-delay="0.5s">
                        <div class="rounded pt-3">
                            <div class="p-4">
                                <h1><span><i class="bi {{key}} {{timestamp}} mb-4 h1"></i></span></h1>
                                <h5>{{chapter}}</h5>
                                <p class="lead fw-normal text-muted mb-3">{{description}}</p>
                            </div>
                        </div>
                    </div>{{/each}}{{/each}}
                </div>
            </div>
            <!-- Service End -->
        </div>
