 <!-- ======= Hero Section ======= -->
<section id="hero" class="d-flex justify-content-center align-items-center">
  <div class="container position-relative wow zoomIn" data-wow-delay="0.1s">
    <h1 class="d-none d-md-block">CIÊNCIA TECNOLOGIA SOCIEDADE</h1>

  {{#each this}}{{#if @first}}{{#each items}}{{#if @first}}
      <center><p>{{title}}</p></center>{{/if}}
  {{/each}}{{/if}}{{/each}}

    <center><a href="/auth/register" class="btn-get-started nav-link">SAIBA MAIS!</a></center>
  </div>
</section><!-- End Hero -->
